Report the model you are currently running as.

Use this to answer questions like "what model are you?" or "which model is this?" — instead of guessing from training data, call this tool to read your actual running identity. It returns your model's human-facing display name, its model id, the upstream model name, and the provider. Takes no parameters.
